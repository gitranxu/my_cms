需求：

配置楼层【维护楼层数据】，

楼层左右移动的也就是在.blocks_move.clear_rx下面的.c_block可以调整宽度，并且他们的宽度(width,c_block之有宽度，没有边框，也没有内边距)，可以设置外边距，总和最多1200

可以设置不独占一行的左右之间的外边距
可以设置独占一行的上下之间的外边距(块组之间，块内楼层之间上下外边距)

新增楼层时，可以选择增加楼层的类型（轮播，导航，普通，综合等）,楼层显示时左上角显示该楼层的类型

模板先手动录入数据库，以后有时间再考虑文件上传方式录入

明天的任务：1.保存时验证，主要是宽度总合验证【完成】
			2.楼层编辑功能的实现【完成】
			3.如果做到这儿的话，开始考虑一进来如果页面中没有#back .cntr元素的话，则弹出新增页面的窗口，进行页面信息收集,
				之前做过收集的页面信息有一个列表，点击列表中的记录则提取出该页的HTML信息
			以上3步如果做完，功能基本上就全通了

周一任务：将bs,b的order从c_blocks,c_block换到c_page_blocks,c_page_block

get_bs_b_css_by_bsid先把这个SQL改了，order取的位置要变

BUG：
a.暂没有楼层，暂没有模板，显示隐藏不对


1.我想知道一个模板被用了多少次，最好能知道是哪些页面的哪个位置[block,floor]引用了
2.编辑数据时用户最关心什么信息

















